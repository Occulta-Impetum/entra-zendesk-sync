# Example non-secret configuration for Entra -> Zendesk Sync.
# Copy to config/config.yaml after running the setup wizard.

entra:
  tenant_id: "00000000-0000-0000-0000-000000000000"
  client_id: "00000000-0000-0000-0000-000000000000"
  selected_groups:
    - group_id: "00000000-0000-0000-0000-000000000000"
      zendesk_org_id: 123456789

zendesk:
  subdomain: "example"
  default_role: "end-user"

behavior:
  suspend_when_out_of_scope: true
  conflict_on_multiple_mapped_groups: true
  dry_run_by_default: true
